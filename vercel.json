{
  "installCommand": "npm ci --prefix simulator-web",
  "buildCommand": "npm run build --prefix simulator-web",
  "outputDirectory": "simulator-web/dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
